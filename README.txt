OpenGML Demo Example

Requires Python 3.9.10 and Kivy.

Needs the kivy environment. Under Linux command line kivy is activated using:
source kivy_venv/bin/activate

To run OpenGML demo from the command line use:
python3 main.py

(Above methods to run a Kivy app under Windows may be different).
